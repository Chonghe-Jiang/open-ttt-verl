# Local dependencies for launching the Modal Polyomino recipes.
#
# Usage:
#   pip install -r requirements-modal.txt
#
# The remote Modal image installs FrontierCS, go-judge, vLLM, torch, and the
# H200-compatible flash-attn wheel inside scripts/modal_polyomino_h200_smoke.py.
# Keep those GPU-runtime pins in the Modal script so local CPU environments do
# not try to install CUDA-specific wheels.
modal
requests
git+https://github.com/FrontierCS/Frontier-CS.git
