# Local dependencies for launching the Modal recipes.
#
# Usage:
#   pip install -r requirements-modal.txt
#
# The remote scripts install task-specific judges, vLLM, torch, and the
# H200-compatible flash-attn wheel in their Modal images. Keep those GPU-runtime
# pins in the launchers so local CPU environments do not install CUDA wheels.
modal
requests
fastapi
PyYAML
git+https://github.com/FrontierCS/Frontier-CS.git
